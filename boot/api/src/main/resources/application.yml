spring:
  datasource:
    url: jdbc:mariadb://192.168.0.17/arte
    driver-class-name: org.mariadb.jdbc.Driver
    username: arte
    password: Arte1234
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update

  servlet:
    multipart:
      enabled: true
      location: C:\update
philo:
  arte:
    upload:
      path: C:\update

security:
  jwt:
    secret: secret-value

UserController:
  signup: Creates user and returns its JWT token
  signin: Authenticates user and returns its JWT token

#  @ApiOperation value값 호출 할수 있게 스웨거 매칭